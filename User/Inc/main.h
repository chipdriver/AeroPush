#ifndef __MAIN_H__                               /* 防止 main.h 被重复包含 */
#define __MAIN_H__                               /* 定义主模块头文件保护宏 */

#include "stm32f4xx.h"                           /* 引入 STM32F4 基础定义 */
#include "app_main.h"                            /* 引入应用层主入口接口 */

#endif                                           /* 结束主模块头文件保护宏 */
